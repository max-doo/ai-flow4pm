# AI Flow 4 PM - 项目总结

> 产品经理的 AI 知识库，探索 AI 技术与产品设计的交汇点

---

## 📖 项目介绍

**AI Flow 4 PM** 是一个基于 Astro 框架构建的个人知识库/博客网站，专注于 AI 技术、产品设计和开发实践的内容分享。项目采用现代化的技术栈，支持 Markdown/MDX 内容创作，并可一键部署到 GitHub Pages。

### 核心特性

| 特性 | 描述 |
|------|------|
| 📝 **多模板系统** | 支持作品(Work)、教程(Tutorial)、笔记(Note)三种内容模板 |
| 📑 **自动目录** | 从 Markdown 标题自动生成可点击的目录导航 |
| 🖼️ **图片预览** | 内置 Lightbox 组件，支持点击放大、缩放、切换、下载 |
| 🏷️ **标签系统** | 文章标签分类、筛选和检索功能 |
| 🎨 **活泼主题** | 四色系（橙/黄/蓝/绿）浅色主题设计 |
| 🚀 **自动部署** | GitHub Actions 自动构建部署到 GitHub Pages |
| 📱 **响应式设计** | 完美适配桌面端和移动端 |

---

## 🏗️ 技术架构

### 技术栈

```
┌─────────────────────────────────────────────────────────┐
│                    前端技术栈                            │
├─────────────────────────────────────────────────────────┤
│  框架        │  Astro 5.x - 静态网站生成器              │
│  样式        │  TailwindCSS 4.x - 原子化 CSS 框架       │
│  内容        │  MDX - 增强的 Markdown（支持组件嵌入）    │
│  语言        │  TypeScript - 类型安全                   │
│  字体        │  Inter + Noto Sans SC + JetBrains Mono   │
└─────────────────────────────────────────────────────────┘
```

### 目录结构

```
ai-flow4pm/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions 部署配置
├── public/
│   ├── img/                    # 静态图片资源
│   ├── favicon.svg             # 网站图标
│   └── logo.svg                # 网站 Logo
├── src/
│   ├── components/             # Astro 组件
│   │   ├── ArticleCard.astro   # 文章卡片
│   │   ├── Breadcrumb.astro    # 面包屑导航
│   │   ├── ImageGallery.astro  # 图片画廊
│   │   ├── ImageLightbox.astro # 图片预览弹窗
│   │   ├── TagList.astro       # 标签列表
│   │   ├── TOC.astro           # 通用目录组件
│   │   └── TutorialTOC.astro   # 教程专用目录
│   ├── content/                # 内容目录（Markdown 文件）
│   │   ├── tutorials/          # 教程文章
│   │   ├── works/              # 作品展示
│   │   └── notes/              # 快速笔记
│   ├── layouts/                # 页面布局
│   │   ├── BaseLayout.astro    # 基础布局
│   │   ├── TutorialLayout.astro# 教程布局
│   │   ├── WorkLayout.astro    # 作品布局
│   │   └── NoteLayout.astro    # 笔记布局
│   ├── pages/                  # 页面路由
│   │   ├── index.astro         # 首页
│   │   └── posts/              # 文章页面
│   │       ├── index.astro     # 文章列表
│   │       └── [...slug].astro # 动态文章详情
│   ├── styles/
│   │   └── global.css          # 全局样式
│   └── content.config.ts       # 内容集合配置
├── astro.config.mjs            # Astro 配置
├── package.json                # 项目依赖
├── tsconfig.json               # TypeScript 配置
└── README.md                   # 项目说明
```

---

## 📝 内容创作指南

### 文章 Frontmatter 规范

所有 Markdown 文件都需要在顶部包含 frontmatter 元数据：

```yaml
---
title: "文章标题"
description: "文章描述/摘要"
date: 2025-11-28
template: tutorial          # work | tutorial | note
tags: ["AI", "产品设计"]
toc: true                   # 是否显示目录
draft: false                # 是否为草稿
author: "作者名"
cover: "/img/xxx/banner.png"  # 封面图路径
---
```

### 教程特有字段

```yaml
---
difficulty: intermediate    # beginner | intermediate | advanced
readingTime: 15            # 预计阅读时间（分钟）
series: "Cursor 系列教程"   # 所属系列
order: 1                   # 系列中的顺序
---
```

### 模板类型说明

| 模板 | 用途 | 特点 |
|------|------|------|
| `work` | 作品展示 | 适合项目案例、设计作品 |
| `tutorial` | 教程文档 | 带侧边目录、难度标识、阅读时间 |
| `note` | 快速笔记 | 简洁布局，默认无目录 |

### 图片使用

1. **存放位置**：将图片放在 `public/img/` 目录下
2. **引用方式**：在 Markdown 中使用相对路径

```markdown
![图片描述](/img/文章目录/图片名.png)
```

3. **图片预览**：布局组件已集成 ImageLightbox，图片会自动支持点击放大

---

## 🚀 开发流程

### 环境准备

```bash
# 1. 克隆项目
git clone https://github.com/你的用户名/ai-flow4pm.git
cd ai-flow4pm

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
```

访问 http://localhost:4321 查看网站

### 常用命令

| 命令 | 说明 |
|------|------|
| `npm run dev` | 启动开发服务器（热更新） |
| `npm run build` | 构建生产版本 |
| `npm run preview` | 预览构建结果 |

### 创建新文章

1. 在对应目录创建 `.md` 文件：
   - 教程：`src/content/tutorials/`
   - 作品：`src/content/works/`
   - 笔记：`src/content/notes/`

2. 添加 frontmatter 和内容

3. 保存后自动刷新预览

---

## 🌐 部署到 GitHub Pages

### 第一步：修改配置

编辑 `astro.config.mjs`，修改以下配置：

```javascript
export default defineConfig({
  site: 'https://你的用户名.github.io',  // 替换为你的 GitHub Pages 地址
  base: '/ai-flow4pm',                    // 替换为你的仓库名
  // ...
});
```

**配置说明：**
- 如果仓库名是 `用户名.github.io`（用户主页），则 `base` 设为 `'/'`
- 如果是普通仓库如 `my-blog`，则 `base` 设为 `'/my-blog'`

### 第二步：推送到 GitHub

```bash
# 初始化 Git（如果尚未初始化）
git init
git add .
git commit -m "Initial commit"

# 添加远程仓库
git remote add origin https://github.com/你的用户名/ai-flow4pm.git

# 推送到 main 分支
git branch -M main
git push -u origin main
```

### 第三步：配置 GitHub Pages

1. 打开 GitHub 仓库页面
2. 进入 **Settings** → **Pages**
3. 在 **Source** 下拉菜单中选择 **GitHub Actions**
4. 保存设置

### 第四步：自动部署

- 每次推送到 `main` 分支会自动触发构建和部署
- 也可以在 **Actions** 页面手动触发 `workflow_dispatch`
- 部署完成后访问：`https://你的用户名.github.io/ai-flow4pm/`

---

## 📋 配置文件说明

### astro.config.mjs

```javascript
export default defineConfig({
  site: 'https://your-username.github.io',  // 网站地址
  base: '/ai-flow4pm',                       // 子路径
  integrations: [mdx()],                     // MDX 支持
  vite: {
    plugins: [tailwindcss()],               // TailwindCSS v4
  },
  markdown: {
    shikiConfig: {
      theme: 'github-dark',                 // 代码高亮主题
      wrap: true,
    },
  },
});
```

### package.json

```json
{
  "name": "ai-flow4pm-blog",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.8.0"
  },
  "devDependencies": {
    "@astrojs/mdx": "^4.2.4",
    "@tailwindcss/typography": "^0.5.16",
    "@tailwindcss/vite": "^4.1.5",
    "tailwindcss": "^4.1.5",
    "typescript": "^5.8.3"
  }
}
```

### .github/workflows/deploy.yml

GitHub Actions 工作流配置，实现：
- 监听 `main` 分支推送
- 使用 Node.js 20 + npm
- 构建 Astro 静态网站
- 部署到 GitHub Pages

---

## 🎨 设计规范

### 色彩系统

| 颜色 | 变量名 | 色值 | 用途 |
|------|--------|------|------|
| 🟠 橙色 | `--color-orange` | #F58220 | 主要交互色、强调 |
| 🟡 黄色 | `--color-yellow` | #FFD500 | 作品模板标识 |
| 🔵 蓝色 | `--color-blue` | #29ABE2 | 教程模板标识 |
| 🟢 绿色 | `--color-green` | #39B54A | 笔记模板标识 |

### 字体

- **标题/正文**：Inter, Noto Sans SC
- **代码**：JetBrains Mono

---

## ✅ 部署检查清单

部署前请确认：

- [ ] `astro.config.mjs` 中的 `site` 和 `base` 已正确配置
- [ ] 所有图片路径使用 `/img/` 开头的相对路径
- [ ] 文章 frontmatter 中 `draft: false`（草稿不会发布）
- [ ] 本地 `npm run build` 构建成功无报错
- [ ] GitHub 仓库 Settings → Pages → Source 设置为 GitHub Actions

---

## 📄 许可证

MIT License

---

## 🔗 相关链接

- [Astro 官方文档](https://docs.astro.build)
- [TailwindCSS 文档](https://tailwindcss.com/docs)
- [MDX 文档](https://mdxjs.com)
- [GitHub Pages 文档](https://docs.github.com/en/pages)

---

*最后更新：2025年11月28日*
